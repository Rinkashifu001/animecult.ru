%main.page
  .container-fluid
    .panel.bg-light

      %h1.heading Создание элемента произведения
      =form_for @element, url: element_index_path do |f|
        =render 'form', f:f
        .row.align-items-center
          .col.text-right=f.submit 'Сохранить', class: 'btn btn-primary'